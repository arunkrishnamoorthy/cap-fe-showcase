using service1 as service from '../../srv/service';

// Hiding this annotation to continue using this filter from step8
annotate service.RootEntities {
    // validFrom @UI.HiddenFilter;
    // validTo @UI.HiddenFilter;
}